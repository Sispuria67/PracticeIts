public static int largestAbsVal (int i, int j, int k) {
    i = Math.abs(i); 
    j = Math.abs(j); 
    k = Math.abs(k); 
    
    if (i >= k && i >= j) {
        return i; 
    
 }else if (j >= i && j >= k) {
      return j;
          
          
  }else{ 
      return k; 
  }
}
